require 'rspec'

##
# to enable color, the following code must be present, and
# one must also use the `-c` flag when using the `rspec` command
RSpec.configure do |config|
  config.tty = true
end
